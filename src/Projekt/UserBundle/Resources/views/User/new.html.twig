{% extends 'ProjektUserBundle::base.html.twig' %}

{% block body -%}
    <h1>Dodawanie uprawnionego</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('user') }}">
            Wstecz (do listy)
        </a>
    </li>
</ul>
{% endblock %}
