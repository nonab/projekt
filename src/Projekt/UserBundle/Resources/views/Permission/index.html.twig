{% extends 'ProjektUserBundle::base.html.twig' %}

{% block body -%}
    <h1>Lista uprawnien</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Kod</th>
                <th>Akcje</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('permission_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.kod }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('permission_show', { 'id': entity.id }) }}">pokaz</a>
                    </li>
                    <li>
                        <a href="{{ path('permission_edit', { 'id': entity.id }) }}">edytuj</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('permission_new') }}">
                Dodaj wpis
            </a>
        </li>
    </ul>
    {% endblock %}
