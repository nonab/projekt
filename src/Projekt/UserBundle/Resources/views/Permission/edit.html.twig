{% extends 'ProjektUserBundle::base.html.twig' %}

{% block body -%}
    <h1>Edycja uprawnien</h1>

    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('permission') }}">
            Wstecz (do listy)
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
